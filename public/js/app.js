var addButton = document.getElementById("showForm");

addButton.addEventListener('click', (res) => {
    alert('hej')
})


myClick = (value) => {
console.log(value)
}


document.addEventListener('DOMContentLoaded', (e) => {





/*
    //const hint = document.querySelector('.allMonkeys');
    const list = document.getElementsByClassName('allMonkeys');
    console.log('hihi');
    console.log(list);





    for(var i = 0; i < list.length; i++){
        console.log(list[i]);
        list[i].style.fontSize = '30px';
    }*/


    
   /* if (hint) {
        console.log(hint);
      const hintShowButton = document.createElement('BUTTON');
      hintShowButton.textContent = 'Show hint';
      hint.parentNode.insertBefore(hintShowButton, hint);
      hint.style.display = 'none';
  
      hintShowButton.addEventListener('click', (e) => {
        hint.style.display = '';
        e.target.style.display = 'none';

        var pos = 0;
        var id = setInterval(frame, 5);
        function frame() {
          if (pos == 100) {
            clearInterval(id);
          } else {
            pos++; 
            hint.style.fontSize = pos + 'px'; 
            hint.style.left = pos + 'px'; 
          }
        }




      })
    }*/
  })